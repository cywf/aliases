# TAK tooling moved

TAK-specific setup assets no longer live in `cywf/aliases`.

Canonical repository:

- <https://github.com/cywf/otg-tak>

Use that repo for TAK server/client setup, Docker/Tailscale/ZeroTier notes, deployment automation, issues, and pull requests.

This pointer remains so older bookmarks to `cywf/aliases/tak` have an observable migration path instead of failing silently.
