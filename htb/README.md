# Hack The Box tooling moved

HTB-specific tooling no longer lives in `cywf/aliases`.

Canonical repository:

- <https://github.com/cywf/htb-helper>

Use that repo for HTB setup workflows, Nmap/payload helpers, CTF scripts, documentation, issues, and pull requests.

This pointer remains so older bookmarks to `cywf/aliases/htb` have an observable migration path instead of failing silently.
